# Database connection string
DATABASE_URL=mongodb://127.0.0.1/your-database-name

# Or use a PG connection string
#DATABASE_URL=postgresql://127.0.0.1:5432/your-database-name

# Used to encrypt JWT tokens
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Used to configure CORS, format links and more. No trailing slash
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Secret used to authenticate cron jobs
CRON_SECRET=YOUR_CRON_SECRET_HERE

# Used to validate preview requests
PREVIEW_SECRET=YOUR_SECRET_HERE

# SMTP for outgoing form notifications (the "Emails" panel on each Form).
# Send through the practice's own mailbox — no third-party service needed.
# Leave SMTP_HOST empty and Payload logs emails to the console instead;
# submissions are always stored under Form Submissions regardless.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=hello@smile360chicago.com
SMTP_PASS=YOUR_APP_PASSWORD_HERE

# Who the notification appears to come from, and where it lands.
EMAIL_FROM_NAME=Smile360 Chicago
EMAIL_FROM_ADDRESS=hello@smile360chicago.com
CONTACT_TO=hello@smile360chicago.com
